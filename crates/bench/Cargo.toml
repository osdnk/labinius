[package]
name = "bin-ntt-bench"
version.workspace = true
edition.workspace = true
description = "The reference round, the calibration runs and the shared timing helpers."
default-run = "bin-ntt"

[dependencies]
bin-ntt.workspace = true

[features]
labrador = ["bin-ntt/labrador"]
